# Создание шифра Цезаря
def encrypt(s, k): # Создание функции encrypt
    letters = "АБВГДЕЁЖЗИЙКЛМНОПРСТУФХЦЧШЩЪЫЬЭЮЯ" # Алфавит из 33 символов
    s = s.upper() # Переводим строку в верхний регистр
    letters2 = "" # Доп. переменная с пустой строкой
    for i in s: # Цикл выводит поочерёдно буквы в переменную i
        if i in letters: # Условие проверки наличия буквы в алфавите
            # Переменная, что содержит в себе новый индекс для буквы с шагом k который указали
	    n = letters.index(i) + k
	    # С помощью оператора остатка от деления % убераем
	    # вероятнось индекса выйти за пределы количества символов в алфавите 
            letters2 += letters[n % len(letters)] 
        else:
            letters2 += i # Если символа нет в алфавите то добавляем его в строку в изночальной форме.
    return letters2 # Вывод шифра
print(encrypt("Привет, мир!", 250))